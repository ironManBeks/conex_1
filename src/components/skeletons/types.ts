import { ReactNode } from "react";

export type TSkeletonLayout = {
    className?: string;
    classPrefix?: string;
    children: ReactNode;
};

export type TSkeleton = {
    className?: string;
};
